import { VisualProposalsLab } from "@/features/mockups/visual-proposals-lab";

export const metadata = {
  title: "Visual proposals lab | Lean Agent Builder",
  description: "High-fidelity visual exploration for the Lean Agent Builder design system.",
};

export default function Page() {
  return <VisualProposalsLab />;
}
