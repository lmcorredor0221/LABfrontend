/**
 * Theme 01 exceptions must be specific, visually legitimate and documented.
 * Keep this list empty whenever possible. Line-based exceptions are intentionally
 * strict so a moved or copied residue is audited again.
 *
 * Shape:
 * { file: "src/...", line: 1, rule: "large_radius", value: "rounded-2xl", reason: "..." }
 */
export const enterpriseThemeAllowlist = [];
